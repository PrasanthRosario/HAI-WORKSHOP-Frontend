/**
 * Environment configuration for development
 * This file contains environment-specific settings and configuration values
 */

export const environment = {
  // Flag indicating whether the application is running in production mode
  production: false,

  // Base URL for the API endpoints in development environment
  // Points to local development server running on localhost port 8000
  apiUrl: 'http://127.0.0.1:8000'
};